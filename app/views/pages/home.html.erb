<h3><%= current_user.user_name if current_user %></h3>

<%= link_to "Login", new_user_session_path  if !current_user %>
<%= link_to "Sign Up", new_user_registration_path if !current_user %>
<%= link_to "Log Out", destroy_user_session_path, method: :delete if current_user %>
